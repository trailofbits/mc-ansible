manticore_script: multiple-styles-solve.py # Manticore script to be run
remote_uname: dropletuser
working_dir: # Replace this with your cwd
logfile: "{{ working_dir }}/manticore_logs"
results_dir: manticore_results # Where manticore results are to be stored


